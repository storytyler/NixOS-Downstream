{ ... }:
{
  home-manager.sharedModules = [
    (_: {
      # Stub: Caelestia bar support
      # Flesh out when switching to caelestia-shell:
      #   - caelestia flake input already in flake.nix
      #   - Deploy config via xdg.configFile or use home-manager module
    })
  ];
}
