{ ... }:
{
  home-manager.sharedModules = [
    (_: {
      # Stub: Noctalia bar support
      # Flesh out when switching to noctalia-shell:
      #   - noctalia flake input already in flake.nix
      #   - Deploy config via xdg.configFile or use home-manager module
    })
  ];
}
